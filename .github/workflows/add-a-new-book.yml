name: Add a new book
on:
  issues:
    types: [opened, edited]
jobs:
  check-bats-version-and-echo-issue-body:
    runs-on: ubuntu-latest
    steps:
      run: |
        echo "Issue title: ${{ github.event.issue.title }}"
        echo "Issue body: ${{ github.event.issue.body }}"
